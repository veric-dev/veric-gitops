apiVersion: v1
kind: Service
metadata:
  name: buildkit
  labels:
    app.kubernetes.io/name: buildkit
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: tcp
      protocol: TCP
      name: tcp
  selector:
    app.kubernetes.io/name: buildkit
