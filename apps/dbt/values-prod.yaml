enabled: false
schedule: "15 */2 * * *"   # every 2 hours
env:
  DBT_PROFILES_DIR: /app/.dbt
  DBT_TARGET: prod
resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    cpu: 4000m
    memory: 4Gi
args:
  - |
    dbt deps && dbt build --target ${DBT_TARGET:-prod}
