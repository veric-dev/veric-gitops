{{- if .Values.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: kernel
  labels:
    {{- include "kernel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "kernel.selectorLabels" . | nindent 4 }}
{{- end }}
